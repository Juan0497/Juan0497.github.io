import React from "react";
import CV from "../../../styles/imgs/CVNE.pdf";

const NavbarModal = () => {
    return(
        <div>
                <img href={CV} alt={CV}/>
        </div>
    );
}

export default NavbarModal;